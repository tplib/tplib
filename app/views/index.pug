extends layout.pug

block scripts
  link(rel='stylesheet', href='/index.css')

block content
  table
    tr
    each book in book_list
      tr
        td#title #{book.title}
        td#author #{book.author}
        td
          if book.format.pdf
            a(href= book.format.pdf) PDF
        td
          if book.format.epub
            a(href= book.format.epub) EPUB
        td
          if book.format.mobi
            a(href= book.format.mobi) MOBI